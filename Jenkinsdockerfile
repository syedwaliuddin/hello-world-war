pipeline {
    agent none
    stages {
        stage ('build stage') {
            agent { label 'docker1' }
            steps {
                sh 'docker build -t mytomimage .'
            }
        }
        stage ('publish stage') {
            agent {label 'docker1'}
            steps {
                sh 'docker login -u sangmeshbhure -p aws@123bhure'
                sh 'docker tag mytomimage sangmeshbhure/tomproject:latest'
                sh 'docker push sangmeshbhure/tomproject'
            }
        }
        stage ('deploy stage') {
            agent { label 'docker2' }
            steps {
                sh 'docker pull sangmeshbhure/tomproject'
                sh 'docker rmi -f tomproject:latest'
                sh 'docker run -d -p 8080:8080 tomproject:latest'
            }
        }
    }
}    
